# Environment Variables Example
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# GitHub Token for sending logs to GitHub Issues
# Get a token from: https://github.com/settings/tokens
# Required permissions: repo (for creating issues)
GITHUB_TOKEN=your_github_token_here

# ============================================
# Android Release Signing (for CI/CD)
# ============================================
# These are used in GitHub Actions workflows for automated signing
# For local builds, use android/key.properties instead (see android/key.properties.example)
#
# To get these values:
# 1. Find your keystore alias: keytool -list -keystore /path/to/keystore.jks
# 2. Encode keystore to base64: base64 -i /path/to/keystore.jks
# 3. Add these as GitHub Secrets (not in .env file):
#    - KEYSTORE_FILE (base64-encoded keystore)
#    - KEYSTORE_PASSWORD (your keystore password)
#    - KEY_ALIAS (the alias name from step 1)
#    - KEY_PASSWORD (your key password, may be same as keystore password)
#
# Note: These are NOT used in local development.
# For local builds, create android/key.properties (see android/key.properties.example)
